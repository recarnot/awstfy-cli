const fs = require('fs');
const path = require('path');
const shell = require('shelljs');

class ProfileModel {
    constructor() {
    }

    
    
}

const profilemodel = new ProfileModel();
exports.profilModel = profilemodel;
